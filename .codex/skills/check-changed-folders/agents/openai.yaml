interface:
  display_name: "Check"
  short_description: "Run format/lint/typecheck/test/build and enforce .claude/.codex folder parity."
  default_prompt: "Identify what changed versus main, enforce .claude/.codex folder parity when either folder changed, run the package checks (format, lint, typecheck, test, build), then summarize pass/fail results."

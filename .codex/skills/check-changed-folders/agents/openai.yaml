interface:
  display_name: "Check"
  short_description: "Run typecheck, tests, and build for the rudder package and enforce Claude/Codex sync."
  default_prompt: "Identify what changed versus main, enforce Claude/Codex parity, run the package checks (typecheck, test, build), then summarize pass/fail results."

interface:
  display_name: "Check"
  short_description: "Run checks and verify attribution and config sync."
  default_prompt: "Use $check-changed-folders to verify agent attribution and Claude/Codex parity, then run typecheck, tests, and build."
